.row-fluid
  .span8.offset2
    .hero-unit
      %h2
        Sorry
      %p.lead
        Signing up with new accounts directly is not supported.  You will receive signup instructions if you are the primary contact for an invitation or when the primary contact adds you to the guest list with an email address.
      = render "devise/shared/links"
