!!! 5
/ paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither/
/[if lt IE 7 ] <html class=\"ie6\" lang=\"en\">
/[if IE 7 ]    <html class=\"ie7\" lang=\"en\">
/[if IE 8 ]    <html class=\"ie8\" lang=\"en\">
/[if IE 9 ]    <html class=\"ie9\" lang=\"en\">
/[if (gt IE 9)|!(IE)] <!--> <html lang=\"en\">

%head
  %meta{ :charset => "utf-8" }  
  -# Set the viewport width to device width for mobile
  %meta{ :name => "viewport", :content => "width=device-width, initial-scale=1.0" }
  %title= content_for?(:title) ? yield(:title) : render_default_title
  
  -# Include files
  = include_files
  = yield :head
  
%body{style: 'margin-top: 50px'}
  = yield :google_maps_scripts
  .container
    = render_top_nav
    = render_alert_boxes
    = yield :header_content
    = yield
%footer.text-center
  %br
  %small
    If you are the copyright holder of any images on this sight and would like me to remove said images please
    = link_to "contact", "/pages/contact"
    me!

:coffee
  $('.modal').modalResponsiveFix(debug: true, spacing: 10)
  $('.modal').touchScroll()